fastapi==0.115.4
uvicorn[standard]==0.32.0
pillow>=10.0.0
numpy>=1.24.0
# Install torch/torchvision appropriate to your platform:
# CPU-only (Windows): pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# Or pick CUDA build if your host supports GPUs
